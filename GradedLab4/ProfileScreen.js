import react from 'react';
import {View, Text, Image, ScrollView, SafeAreaView, Inputtext} from 'react-native';


export default function ProfileScreen(navigator) {

  return(
    <ScrollView>
    
    
    </ScrollView>
  )
}